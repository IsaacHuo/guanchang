services:
  neo4j:
    image: neo4j:5.12.0
    container_name: guanchang-neo4j
    ports:
      - "7474:7474"   # HTTP 浏览器管理界面
      - "7687:7687"   # Bolt 驱动端口
    environment:
      - NEO4J_AUTH=neo4j/password
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data: